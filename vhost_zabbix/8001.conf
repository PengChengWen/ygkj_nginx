
server {
	listen 8001;
	server_name localhost;
	
	location / {
		root /var/tools/zabbix;
		index index.php index.html index.htm;
	}

	location ~ \.php {
                root /var/tools/zabbix;
                include /etc/nginx/fastcgi.conf;
		fastcgi_pass 127.0.0.1:9001;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
}
